# Keeper Standalone Environment Configuration
# Copy this file to .env and fill in the required values

# =============================================================================
# REQUIRED: Appdata path
# =============================================================================

# APPDATA: path to the directory where postgresql data will be stored
APPDATA=/mnt/user/home

# =============================================================================
# REQUIRED: Security & Encryption
# =============================================================================

# BETTER_AUTH_SECRET: Secret key for Better Auth session signing and encryption.
# Generate with: openssl rand -base64 32
# This is critical for authentication security.
BETTER_AUTH_SECRET=

# ENCRYPTION_KEY: 32-byte key for encrypting sensitive data at rest.
# Generate with: openssl rand -hex 32
# Used to encrypt secrets, API keys, and other sensitive configuration.
ENCRYPTION_KEY=

# =============================================================================
# REQUIRED: CORS & Security Origins
# =============================================================================

# TRUSTED_ORIGINS: Comma-separated list of allowed origins for CORS.
# Required if not accessing from localhost.
# Example: https://keeper.example.com,https://app.example.com
# For localhost development, you can leave this blank.
TRUSTED_ORIGINS=

# =============================================================================
# OPTIONAL: Commercial Features
# =============================================================================

# COMMERCIAL_MODE: Enable commercial/paid features (default: false).
# Set to "true" to enable billing, subscriptions, and commercial functionality.
COMMERCIAL_MODE=false

# =============================================================================
# OPTIONAL: OAuth Providers (Google)
# =============================================================================

# GOOGLE_CLIENT_ID: OAuth 2.0 Client ID from Google Cloud Console.
# Required only if enabling Google authentication.
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=

# GOOGLE_CLIENT_SECRET: OAuth 2.0 Client Secret from Google Cloud Console.
# Keep this secret - do not commit to version control.
GOOGLE_CLIENT_SECRET=

# =============================================================================
# OPTIONAL: OAuth Providers (Microsoft)
# =============================================================================

# MICROSOFT_CLIENT_ID: Application (client) ID from Azure AD/App registrations.
# Required only if enabling Microsoft authentication.
# Register at: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
MICROSOFT_CLIENT_ID=

# MICROSOFT_CLIENT_SECRET: Client secret from Azure AD/App registrations.
# Keep this secret - do not commit to version control.
MICROSOFT_CLIENT_SECRET=

# =============================================================================
# NOTES: Standalone Mode
# =============================================================================
# The following variables are NOT required in standalone mode as they are
# handled internally by the keeper-standalone image:
#
# - WEBSOCKET_URL: Not needed (internal WebSocket server included)
# - API_URL: Not needed (API is served from the same container)
# - NEXT_PUBLIC_* variables: Not required for standalone operation
#
# The standalone image bundles the web UI, API, database, and WebSocket server
# into a single container for simplified deployment.
